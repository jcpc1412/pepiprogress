/// <reference types="expo/types" />

// Committed twin of the auto-generated (and git-ignored) `expo-env.d.ts`.
// It pulls in Expo's ambient declarations — including CSS / CSS-module imports
// (`expo/types/global.d.ts`) — so `tsc` resolves them in CI, where the generated
// file doesn't exist. Both referencing `expo/types` is idempotent (no conflict).
